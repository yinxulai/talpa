/* eslint-disable */

// 支持编码转出的图片类型
export enum SupportedEncodeMimeType {
  PNG = 'image/png',
  JPEG = 'image/jpeg'
}

// 支持解码输入的图片类型
export enum SupportedDecodeMimeType {
  PNG = 'image/png',
  JPEG = 'image/jpeg',
  HEIC = 'image/heic',
  TIFF = 'image/tiff',
  GIF = 'image/gif'
}

/* eslint-enable */
